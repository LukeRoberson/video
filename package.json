{
  "name": "video-frontend",
  "version": "1.2.0",
  "description": "TypeScript frontend for video streaming application",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf static/js/dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}